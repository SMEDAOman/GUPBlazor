namespace GUPBlazor.Components;

/// <summary>
/// Whether the filter chip renders as a button (default — closeable) or a link.
/// Mirrors gup-filter-chip's <c>kind</c> attribute.
/// </summary>
public enum GupFilterChipKind
{
    Button,
    Link,
}

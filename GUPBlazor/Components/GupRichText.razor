@* Equivalent of <gup-rich-text> — wraps HTML content with typography styles *@
<div class="gup-rich-text">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}

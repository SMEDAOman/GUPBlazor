/**
 * GUP Forms - Shared class-based constants
 *
 * Common CSS class names shared across all lite form components.
 * Import the corresponding forms.css alongside any form component CSS.
 */

export const gupFormClasses = {
  /** Hint text below a field or group */
  hint: 'gup-form-hint',
  /** Error message with icon before a group's options */
  error: 'gup-form-error',
} as const;

/**
 * GUP Components - Class-based approach
 *
 * This module provides an alternative way to use GUP component styles
 * with native HTML elements instead of web components.
 *
 * @example
 * // Import the utility class
 * import { GupButton } from '@govom/components/classes';
 *
 * // Import the CSS in your app's entry point
 * import '@govom/components/classes/button/button.css';
 */

export * from './button';
export * from './checkbox';
